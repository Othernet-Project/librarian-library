// Generated by CoffeeScript 1.10.0
(function(window, $, templates) {
  var libraryItemLinkSelector, libraryList, openerPanelSelector;
  libraryList = $('#library-list');
  libraryItemLinkSelector = '.library-item-link';
  openerPanelSelector = '#content';
  $('#content-language').on('change', function() {
    $(this).parents('form').submit();
  });
  return libraryList.on('click', libraryItemLinkSelector, function(e) {
    var href, link, res;
    e.preventDefault();
    link = $(this);
    href = link.attr('href');
    res = $.get(href);
    res.done(function(url) {
      return $.modalContent(url, {
        fullScreen: true
      });
    });
    res.fail(function() {
      return $.modalDialog(templates.loadFailure);
    });
  });
})(this, this.jQuery, this.templates);
